import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import SignUpUser from './components/SignUpUser';  // Ensure the path is correct

ReactDOM.render(
  <React.StrictMode>
    <SignUpUser />
  </React.StrictMode>,
  document.getElementById('root')
);
